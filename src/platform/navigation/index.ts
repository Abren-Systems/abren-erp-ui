/**
 * Navigation / Workspace
 *
 * Persistent workspace management — open screen instances, dirty state
 * tracking, and recently-viewed support.
 */

export type { ScreenInstance, Workspace } from './workspace.types'
