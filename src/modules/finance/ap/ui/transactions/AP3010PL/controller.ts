/**
 * AP3010PL — Payment Requests Primary List Controller
 *
 * Manages the behavioral lifecycle for the Payment Requests List screen.
 * Orchestrates data loading, filtering, selection, and side panel context.
 *
 * screen.ts = metadata (what the screen IS)
 * controller.ts = behavior (what the screen DOES)
 */

// Placeholder: Controller logic will be extracted from view.vue in Phase 4
export const AP3010PL_CONTROLLER_VERSION = 1
